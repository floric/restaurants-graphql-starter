import { css } from "styled-components";
import { Colors } from "./colors";

export const inputPadding = css`
  padding: 0.5rem;
`;

export const inputShadow = css`
  border: none;
  background: none;
  box-shadow: 0 0.1rem ${Colors.GRAY_MEDIUM};
`;
